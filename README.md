# Synchronized time of flight for dark environments

## Building

Either select the "Build Active Project" option in the VSCode QNX Extension under "QNX Commands", or run:

```bash
make BUILD_PROFILE=debug PLATFORM=aarch64le all 
```
